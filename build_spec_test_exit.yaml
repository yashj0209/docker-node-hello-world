version: 0.1
component: build
timeoutInSeconds: 2000
runAs: root
shell: bash

steps : 
  - type: Command
    name: "echo test"
    timeoutInSeconds: 2000
    command: |
      echo "pwd test"
    onFailure:
      - type: Command
        command: |
          echo "Handling Failure"
          echo "Failure successfully handled"
        timeoutInSeconds: 1000
        
  - type: Command
    name: "docker test"
    timeoutInSeconds: 2000
    command: |
      docker build .
    onFailure:
      - type: Command
        command: |
          echo "Handling Failure 2"
          echo "Failure successfully handled 2"
        timeoutInSeconds: 1000
    
